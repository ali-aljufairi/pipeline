# pipeline

Modernized from Create React App → Vite.

## Development

```bash
npm install
npm run dev
```

## Lint / Format

```bash
npm run lint
npm run format
```

## Build / Preview

```bash
npm run build
npm run preview
```

## Docker

```bash
docker build -t pipeline .
docker run --rm -p 8080:80 pipeline
```
